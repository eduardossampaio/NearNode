CREATE TABLE profileEntity (
    id TEXT NOT NULL PRIMARY KEY,
    username TEXT NOT NULL,
    ip TEXT NOT NULL,
    status TEXT NOT NULL
);

updateStatus:
UPDATE profileEntity SET status = ? WHERE id = ?;

getProfile:
SELECT * FROM profileEntity WHERE id = ?;

insertOrUpdate:
INSERT OR REPLACE INTO profileEntity(id, username, ip, status)
VALUES ?;
